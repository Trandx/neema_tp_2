command to build and remove orphans container
    sudo docker-compose up -d --build --force-recreate --no-deps --remove-orphans 

commad to delete <none> container
    docker rmi $(docker images -f "dangling=true" -q) -f

    ### passport: active uuids

    php artisan passport:install --uuids
    
command to give permission on folder and subfolder
	
	sudo chmod -R 777 .